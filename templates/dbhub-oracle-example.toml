# dbhub.toml — Oracle example
# Copy to your project root and adjust the connection values.
# Use environment variables for passwords: ${ORACLE_PASSWORD}

[[sources]]
id = "main"
dsn = "oracle://readonly_user:${ORACLE_PASSWORD}@dbhost.example.com:1521/MYSERVICE"

[[tools]]
name = "execute_sql"
source = "main"
readonly = true
